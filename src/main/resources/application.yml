spring:
  application:
    name: help-desk-backend

  datasource:
    url: jdbc:mysql://localhost:3306/spring_ai
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true

    properties:
      hibernate:
       dialect: org.hibernate.dilect.MySQL8Dialect
        #format_sql: true

server:
  port: 8081